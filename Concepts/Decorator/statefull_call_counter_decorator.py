"""
8) ------- Stateful call counter
Create a decorator call_counter that attaches an attribute call_count to the wrapped function. 
Each invocation increments this counter, so after three calls myfunc.call_count == 3. The counter 
lives on the wrapper object itself.
"""
