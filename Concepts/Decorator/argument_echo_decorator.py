"""
3) ------Argument-echo decorator
Create show_args that prints the positional arguments tuple and the 
keyword-argument dictionary each time the function is called, then forwards the call unchanged. 
The output should look like args=(1, 2) kwargs={'c': 3} for a call such as func(1, 2, c=3).
"""
