"""
2) ------- Timing decorator 
    Implement timer that measures how long the wrapped function runs. After the function returns, 
    print Elapsed: X.XXXs where the elapsed time is formatted to three decimal places. 
    You'll need time.perf_counter() (or time.time()) and the usual wraps boilerplate.
"""

