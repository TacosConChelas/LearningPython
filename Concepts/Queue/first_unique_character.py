"""
    Exercise 3 — First Unique Character in a Stream

Description:
    Given a stream of characters (one at a time), return the first non-repeating character at each step.
    Use a queue to track potential unique characters.
Hint:
    Use a queue + dictionary (dict or Counter) to keep track of counts.
    If a character’s count > 1, remove it from the queue.
"""
