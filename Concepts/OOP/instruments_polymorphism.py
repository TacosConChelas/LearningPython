"""
    Exercise 3
Write a function play_instrument(instrument) that calls .play().
Then create Guitar, Piano, and Drum classes — each with their own play() method.
Show duck typing by calling the function on different instrument objects.
"""